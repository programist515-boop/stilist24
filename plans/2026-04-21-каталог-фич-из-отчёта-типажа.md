# Каталог фич из отчёта «Типаж Гамин-Драматик»

Дата создания: 2026-04-21
Последний апдейт: 2026-04-21 (scope: оставлены фичи Этапа 1, beauty/gender вынесены в Этап 2)
Источник: PDF-гайд по подтипу Гамин-Драматик (Flamboyant Gamine по Кибби).

## Контекст

PDF — один из 13 подтипов. Для каждого подтипа существует аналогичный по структуре гайд. План фиксирует все фичи/правила, которые мы извлекли, с пометкой текущего состояния в коде, и задаёт порядок реализации.

Маппинг подтипа PDF → наш код: «Гамин-Драматик» = `flamboyant_gamine` в [identity_subtypes.yaml](../ai-stylist-starter/config/rules/identity_subtypes.yaml).

## Легенда статусов

- `[x]` — реализовано
- `[~]` — частично реализовано
- `[ ]` — не реализовано

---

# ЭТАП 1 (ближайший релизный цикл)

## Фаза 0. Фундамент: расширение атрибутов вещи

Без этих атрибутов большинство правил ниже применять не к чему. Миграция `Item` + обновление `cv_feature_extractor.py` + расширение `garment_recognition_rules.yaml`.

- [x] `fabric_rigidity` (soft / medium / rigid) — «ткань держит форму»
- [x] `fabric_finish` (matte / glossy / metallic / sequin / brocade) — матовая vs глянцевая, вечер
- [x] `occasion` (day / work / smart_casual / evening / sport) — вечерний режим как отдельная ось
- [x] `neckline_type` (v / straight / boat / round / sweetheart / halter / asymmetric / turtleneck / off_shoulder)
- [x] `sleeve_type` (set_in / raglan / batwing / dropped_shoulder / cap / puff_sharp / sleeveless)
- [x] `sleeve_length` (short / three_quarter / long_wrist / long)
- [x] `pattern_scale` (small / medium / large)
- [x] `pattern_character` (geometric / abstract / floral_soft / floral_bold / watercolor / animal / stripe / dots / checks / asymmetric)
- [x] `pattern_symmetry` (symmetric / asymmetric)
- [x] `detail_scale` (small / medium / large) — кросс-режущий атрибут: сумки, украшения, фурнитура
- [x] `structure` (structured / semi_structured / unstructured)
- [x] `cut_lines` (angular / straight / soft_flowing) — угловатый vs плавный крой
- [x] `shoulder_emphasis` (required / neutral / avoided) — для правил «плечи всегда выделены»
- [x] `style_tag[]` (military / preppy / dandy / casual / smart_casual / dramatic / twenties / romantic) — связка с Фазой 6
- [x] Alembic-миграция (0009) + эвристики в `garment_recognizer.py` + `garment_recognition_rules.yaml`. Примечание: `cv_feature_extractor.py` — это анализ лица/позы пользователя, не одежды; корректно расширять `garment_recognizer.py` (`recognize_extended`).

Критерий готовности: CV-анализ новой вещи заполняет все 14 полей хотя бы эвристикой, при недостатке данных — честный `quality=low` (см. design_philosophy).

---

## Фаза 1. Правила силуэта и линий

Отдельный YAML — [silhouette_rules.yaml](../ai-stylist-starter/config/rules/silhouette_rules.yaml). Правила — per-subtype с наследованием от семейства.

- [x] Контрастный верх/низ, разбитый силуэт — вынесено в явное правило в silhouette_rules.yaml (FG)
- [x] Угловатость vs плавность — как правило в YAML (требует Фазы 0 для применения сервисом)
- [x] Смешивание противоположных форм — явное composition_rule `narrow_base_plus_wide_opposite`
- [x] Крой (прямой / полуприлегающий / облегающий)
- [x] «Акцент на плечи / без реглана» — в prefer/avoid блоках YAML
- [x] [services/silhouette_rules_service.py](../ai-stylist-starter/app/services/silhouette_rules_service.py) — применяет prefer/avoid/composition_rules на уровне образа; scorer-обёртка [outfits/scoring/silhouette_rules.py](../ai-stylist-starter/app/services/outfits/scoring/silhouette_rules.py) зарегистрирована в `OutfitScorer._DEFAULT_SCORERS` (вес 0.10, понижается до 0.03 при quality=low). Цветовые композиционные правила (broken_by_color/unbroken_vertical) сознательно отданы `ColorCombinationService` — разделение ответственности.
- [x] Наполнен контент остальных 12 подтипов в silhouette_rules.yaml (2026-04-25): по 4–7 prefer, 4–6 avoid, 4–5 composition_rules на подтип

---

## Фаза 2. Правила категорий одежды

Каждая категория — отдельный YAML с правилами «да/нет» для подтипа + стоп-лист (см. Фазу 3). Формат единый.

- [x] [category_rules/jackets.yaml](../ai-stylist-starter/config/rules/category_rules/jackets.yaml) — FG наполнено, 12 подтипов placeholder
- [x] [category_rules/dresses.yaml](../ai-stylist-starter/config/rules/category_rules/dresses.yaml) — FG, placeholder
- [x] [category_rules/sweaters.yaml](../ai-stylist-starter/config/rules/category_rules/sweaters.yaml) — FG, placeholder
- [x] [category_rules/swimwear.yaml](../ai-stylist-starter/config/rules/category_rules/swimwear.yaml) — FG, placeholder
- [x] [category_rules/pants.yaml](../ai-stylist-starter/config/rules/category_rules/pants.yaml) — FG, placeholder
- [x] [category_rules/skirts.yaml](../ai-stylist-starter/config/rules/category_rules/skirts.yaml) — FG, placeholder
- [x] [category_rules/outerwear.yaml](../ai-stylist-starter/config/rules/category_rules/outerwear.yaml) — FG с разбивкой на coat/fur_coat/puffer/biker_jacket/trench
- [x] [category_rules/headwear.yaml](../ai-stylist-starter/config/rules/category_rules/headwear.yaml) — FG
- [x] [category_rules/shoes.yaml](../ai-stylist-starter/config/rules/category_rules/shoes.yaml) — FG
- [x] [category_rules/bags.yaml](../ai-stylist-starter/config/rules/category_rules/bags.yaml) — FG
- [x] [category_rules/belts.yaml](../ai-stylist-starter/config/rules/category_rules/belts.yaml) — FG + условное правило «если талии нет — пропустить»
- [x] [category_rules/eyewear.yaml](../ai-stylist-starter/config/rules/category_rules/eyewear.yaml) — FG
- [x] [category_rules/jewelry.yaml](../ai-stylist-starter/config/rules/category_rules/jewelry.yaml) — FG (count_per_outfit 1–2)
- [x] [category_rules/hosiery.yaml](../ai-stylist-starter/config/rules/category_rules/hosiery.yaml) — FG (день/вечер + leg_line_strategy)
- [x] [services/category_rules_service.py](../ai-stylist-starter/app/services/category_rules_service.py) — единый движок: per-item score с `prefer`-атрибутами, алиасы категорий (jeans→pants, blazer→jackets и т.п.); scorer [outfits/scoring/category_rules.py](../ai-stylist-starter/app/services/outfits/scoring/category_rules.py) зарегистрирован (вес 0.10). `stop` поддерживает поле `match` — структурированные predicates для штрафов (AND-семантика, списки = OR). Для FG заполнены 23 predicates в jackets/pants/shoes/dresses/skirts.
- [x] Наполнены все 12 подтипов во всех 15 категориях (2026-04-25): топ-4 (`jackets`, `dresses`, `pants`, `shoes`) в сессии 5 + остальные 11 (`bags`, `belts`, `blouses`, `eyewear`, `headwear`, `hosiery`, `jewelry`, `outerwear`, `skirts`, `sweaters`, `swimwear`) в сессии 6. Каждый блок: prefer (4–8 атрибутов) + stop с `reason` (3–5 правил, часть с structured `match`-predicates). 782 passed.

Зависит от Фазы 0 для применения сервисом; YAML-контент написан впереди.

---

## Фаза 3. Стоп-листы как единая структура

Оси: силуэт, детали, ткани, принты, цвет, категории. Все элементы содержат `reason` для explainer.

- [x] [stop_lists/flamboyant_gamine.yaml](../ai-stylist-starter/config/rules/stop_lists/flamboyant_gamine.yaml) — единый формат для FG
- [x] Стоп-лист тканей (грубая шерсть, тонкий трикотаж, мерцающие нежные) — в файле выше
- [x] Стоп-лист принтов (акварельные, мелкие симметричные, мягкие волны, цветочные мягкие)
- [x] Стоп-лист силуэтов (бесформенные, плавные, симметричные, «хорошая девочка»)
- [x] Стоп-лист деталей (драпировки, оборки, округлые формы)
- [x] Стоп-лист цветов (моноцвет, пастели, пыльные тёплые, нейтральные)
- [x] Стоп-лист категорий — содержится в `stop` блоках category_rules/*, `categories_ref` ссылается на них
- [~] Human-readable explainer: связать `explainer.py` с `stop_lists` — сейчас explainer работает без типажной привязки
- [ ] Фронт — красный блок «тебе НЕ идёт» в разборе вещи и лука
- [x] Наполнить stop_lists/<subtype>.yaml для остальных 12 подтипов (2026-04-25): по 5 осей × 3–5 правил с `reason` на каждое

---

## Фаза 4. Цветовая теория и сочетания

Расширяем `color_engine` и добавляем новый модуль сочетаний.

- [x] Колор-палитра цветотипа (12 сезонов)
- [x] [color_schemes.yaml](../ai-stylist-starter/config/rules/color_schemes.yaml) — триада, аналоговая, комплиментарная, split-complementary, tetradic, monochromatic
- [x] [services/color_combination_service.py](../ai-stylist-starter/app/services/color_combination_service.py) — проверяет и предлагает сочетания для образа; scorer-обёртка [outfits/scoring/color_combination.py](../ai-stylist-starter/app/services/outfits/scoring/color_combination.py) зарегистрирована в `OutfitScorer._DEFAULT_SCORERS` (вес 0.15)
- [x] Правило «разбить силуэт цветом» — composition_rule `break_vertical_by_color` в color_schemes.yaml (FG)
- [x] Стоп-лист цветов (пыльные, пастельные, тёплые нейтральные) — в `forbidden_palettes` и stop_lists/flamboyant_gamine.yaml
- [ ] Explainer для сочетаний: почему это сочетание работает/не работает

---

## Фаза 5. Color try-on — примерка в цветах палитры

- [x] `services/color_try_on_service.py` — CV-перекрашивание: rembg mask + HSV-shift (value сохраняется → тени/блики остаются)
- [x] Ограничение: работает при `solid_color=true` и `pattern_scale=null`. Guard подключён в сессии 9 (2026-04-26): `ColorTryOnService._is_hsv_suitable(item)` проверяет `pattern_scale is None and fabric_finish != 'metallic'`. При не-пригодной вещи и `ENABLE_ML_COLOR_TRYON=false` — пустой ответ + `quality='low'`. При ML on — пропускаем (FASHN расскрасит принт). Legacy-вещи без атрибутов Фазы 0 проходят (back-compat). +6 тестов в `TestHsvSuitabilityGuard`.
- [x] Fallback для принтов/металлика: FASHN recolor, за флагом `ENABLE_ML_COLOR_TRYON` (по умолчанию off — $$, stub-хук `_maybe_run_ml`)
- [x] API `POST /color-tryon/{item_id}` + `GET` + `POST /feedback` → набор изображений по цветам палитры цветотипа
- [x] Фронт — галерея «примерь в N цветах», клик = feedback
- [x] Feedback → `personalization_service.record_color_preference` → учит цветовой вектор предпочтений

Независимая фаза, можно делать параллельно Фазам 1–4.

---

## Фаза 6. Ось «Стили»

- [x] [styles.yaml](../ai-stylist-starter/config/rules/styles.yaml) — справочник: smart_casual, casual, military, dandy, preppy, romantic_adapted, dramatic, twenties (8 стилей)
- [x] Для каждого стиля: core_items, impression, occasion, color_hints
- [x] [style_subtype_affinity.yaml](../ai-stylist-starter/config/rules/style_subtype_affinity.yaml) — affinity для FG (military ++, dramatic ++, twenties ++, остальные +) + score_modifiers
- [x] Scorer [outfits/scoring/style_affinity.py](../ai-stylist-starter/app/services/outfits/scoring/style_affinity.py) интегрирован в `OutfitScorer._DEFAULT_SCORERS` (вес 0.08): читает `style_tags` вещей, применяет affinity-модификаторы, поддерживает `context.selected_style` как фильтр тегов.
- [ ] UI — селектор стиля в «образ на сегодня» и «собери лук» (бэкенд уже поддерживает `selected_style` в context)
- [x] Наполнить affinity для остальных 12 подтипов (2026-04-25): 12 × 8 стилей с уровнями excellent/good/neutral/avoid (никаких neutral-плейсхолдеров)

Зависит от Фазы 0 для применения сервисом; YAML написан впереди.

---

## Фаза 7. Референсные луки и сборка из гардероба

- [x] Каркас [reference_looks/](../ai-stylist-starter/config/rules/reference_looks/) и первый YAML [flamboyant_gamine.yaml](../ai-stylist-starter/config/rules/reference_looks/flamboyant_gamine.yaml) — 5 луков
- [ ] Покрытие остальных 12 подтипов (YAML-на-подтип, по 5–8 луков каждый)
- [ ] `services/reference_matcher.py` — ищет в гардеробе вещи, подходящие по `requires` каждого слота референса
- [ ] API `GET /reference-looks` — возвращает референсы подтипа пользователя + `matched_items[]` + `missing_slots[]`
- [ ] Фронт — страница «Образы по твоему типажу», кнопка «собрать из гардероба»
- [x] Интеграция с `gap_analysis`: `missing_slots` идут в «что докупить» (2026-04-26): `GapAnalysisService` опционально принимает `ReferenceMatcher`, при `user_context["identity_family"]` собирает `missing_slots` всех реф-луков подтипа и добавляет их как `GapSuggestion` с полями `from_reference_look` и `slot_hint`. Дедупликация по `(category, item)`. Расширена схема `GapSuggestion` (опциональные поля). Guard-rail: ошибка matcher'а не ломает анализ. +9 тестов в `test_gap_analysis_service.py`. **827 passed**.
- [x] «Рекомендация покупки под референс» — закрыто той же интеграцией (см. выше)

Зависит от Фазы 0 (атрибуты) и Фазы 2 (правила категорий для валидации matching).

---

## Фаза 8. Ассоциативный слой (identity DNA)

- [x] Создан отдельный [identity_subtype_profiles.yaml](../ai-stylist-starter/config/rules/identity_subtype_profiles.yaml) (чтобы не ломать схему классификатора в identity_subtypes.yaml): `associations[]`, `motto`, `philosophy`, `key_principles[]`
- [x] `celebrity_examples[]` — 8 имён для FG (Одри Хепбёрн, Кристина Орбакайте, Алёна Свиридова, Мэг Райан, Кристал Ренн, Кьяра Ферраньи, Тина Сизонова, Алсу)
- [x] Placeholder-структура для остальных 12 подтипов создана
- [x] Инфраструктура под трек A (LLM-черновик + community-верификация) — см. [plans/enumerated-seeking-fern.md](../../../Users/user/.claude/plans/enumerated-seeking-fern.md):
  - [scripts/phase8_llm_prompt_builder.py](../scripts/phase8_llm_prompt_builder.py) — собирает готовый промпт по `<subtype>` (FG-эталон + семейство + формула). Запуск: `python scripts/phase8_llm_prompt_builder.py <subtype>`
  - [tests/test_identity_subtype_profiles_completeness.py](../ai-stylist-starter/tests/test_identity_subtype_profiles_completeness.py) — валидатор полноты. Placeholder'ы = XFAIL, наполненные = обязательный CI-gate.
- [x] Наполнены все 13 подтипов в [identity_subtype_profiles.yaml](../ai-stylist-starter/config/rules/identity_subtype_profiles.yaml) (2026-04-22). Тест `test_identity_subtype_profiles_completeness` зелёный: 13 passed. Celebrity_examples: 4 западных (Kibbe-verified где возможно) + 4 русских (community_consensus) на подтип. Русские имена помечены как ориентир, а не вердикт Кибби.
- [x] `explainer.py` использует `associations` и `motto` в текстах рекомендаций (2026-04-26): новые функции `get_identity_profile()`, `identity_intro()` + опциональный `subtype` параметр в `explain_outfit()` и `explain_shopping()`. Подключено в `today_service` и `routes/shopping.py` через `user_context.identity_family`. +21 тест в `test_explainer.py`. Identity-фразы добавляются только при положительном вердикте (score≥0.55 для outfit, decision=buy для shopping) — не «комплиментуем» плохие сочетания.
- [ ] Фронт — блок «Кто ты стилистически» с девизом, ассоциациями и галереей звёзд в разделе `analyze` — дисклеймер «примеры по community-консенсусу, не подтверждены Кибби лично»
- [ ] Выборочная ручная проверка 3–5 русских celebrity на спорные подтипы (soft_dramatic/theatrical_romantic/soft_natural) перед релизом

Независимая, в основном контент. Можно делать параллельно ML-фазам.

---

## Порядок Этапа 1 и зависимости

```
Фаза 0 (атрибуты) ──┬── Фаза 1 (силуэт)
                    ├── Фаза 2 (категории)
                    ├── Фаза 6 (стили)
                    └── Фаза 7 (референсы)  ← нужна также Фаза 2

Фаза 3 (стоп-листы)  — нужны данные Фазы 2
Фаза 4 (цвет-теория) — независима
Фаза 5 (color try-on) — независима
Фаза 8 (ассоциации) — независима, контент
```

Рекомендуемый порядок исполнения:
Фаза 0 → Фаза 8 (контент параллельно) → Фаза 2 → Фаза 1 → Фаза 3 → Фаза 7 → Фаза 4 → Фаза 6 → Фаза 5.

---

## Параллелизация Этапа 1

Ключевой принцип: **YAML-контент и код-сервис каждой фазы — это две разные задачи**. Контент с `pending_` атрибутами (как в [flamboyant_gamine.yaml](../ai-stylist-starter/config/rules/reference_looks/flamboyant_gamine.yaml)) пишется впереди кода, сервисы прототипируются на моках. Единственный реальный блокер — миграция `Item` в Фазе 0.

### Треки

| Трек | Профиль исполнителя | Что делает |
|---|---|---|
| **A. Инфра** | backend-инженер | Фаза 0 (миграция + CV) → движки Фаз 2, 1, 6, 3, 7 |
| **B. Контент** | стилист + контент-инженер | Все YAML на `pending_` атрибутах: Фазы 1, 2, 3, 4, 6, 7, 8 |
| **C. Color try-on** | CV-инженер | Фаза 5 целиком — полностью изолированная фича |
| **D. Identity DNA** | контент + простой фронт | Фаза 8 целиком — расширение YAML + UI-карточка |
| **E. Фронт** | frontend-инженер | UI под все фазы по мере готовности API |

### Три волны

**Волна 1 — старт в день 1, всё параллельно:**

- Трек A: Фаза 0 — миграция `Item`, CV-экстрактор 14 новых полей
- Трек B: YAML-наполнение сразу для **Фаз 1, 2, 3, 4, 6, 7** (все 14 категорий, силуэт, стоп-листы ткани/принты/цвет/детали, справочник стилей, 12 YAML референсных луков, цветовые схемы) — всё на `pending_` атрибутах
- Трек C: **Фаза 5** целиком (rembg+HSV, API, фронт) — не зависит ни от чего в плане
- Трек D: **Фаза 8** целиком (`associations`, `motto`, `celebrity_examples[]`, карточка «Кто ты стилистически»)

Результат Волны 1: ~70% работы Этапа 1 закрыто по контенту. Фазы 5 и 8 можно релизить в прод — они не ждут остальных.

**Волна 2 — после того как Фаза 0 в main:**

- Трек A: `category_rules_service` (Фаза 2 движок) + `silhouette_rules_service` (Фаза 1 движок) + интеграция `style_tag[]` в `outfit_engine` (Фаза 6 движок)
- Трек B параллельно: `color_combination_service` (Фаза 4 движок) — не зависит от атрибутов, может делаться параллельно

**Волна 3 — композиционные фичи, требуют данных Волны 2:**

- `stop_lists_service` + explainer (Фаза 3 движок) — собирает данные из category_rules и силуэта
- `reference_matcher` (Фаза 7 движок) — нужны атрибуты + category_rules для валидации матчей
- Финальная интеграция всего в `outfit_engine`

### Зависимости в виде графа

```
Волна 1 (параллельно):
  ┌── A: Фаза 0 (блокер)
  ├── B: YAML Фаз 1,2,3,4,6,7 (pending_ атрибуты)
  ├── C: Фаза 5 целиком ──────────────► можно релизить
  └── D: Фаза 8 целиком ──────────────► можно релизить

Волна 2 (после Фазы 0):
  ┌── A: движки Фаз 2, 1, 6
  └── B: движок Фазы 4 (параллельно)

Волна 3 (после Волны 2):
  ├── движок Фазы 3 (нужна Ф.2)
  └── движок Фазы 7 (нужны Ф.0 + Ф.2)
```

### Практические режимы работы

- **С 1 инженером:** последовательно A-трек (0 → 2 → 7 → остальное), параллельно в фоне B-контент без переключения контекста. Треки C и D — откладываются или отдаются подрядчику.
- **С 2 инженерами:** один на A (инфра → движки), второй на C (изолированный color try-on). Контент — стилист / подрядчик.
- **С 3+ инженерами:** полный параллелизм — треки A, C, D сразу + контент в Треке B.

### Быстрые победы (ship early)

Фазы, которые можно отгрузить в прод **до завершения Волны 2**:

1. **Фаза 5 (color try-on)** — замкнутая, видимый UX-wow, ~1 неделя инженера.
2. **Фаза 8 (identity DNA)** — контент + карточка, 1–2 дня, моментально повышает «комплексность» в UX.
3. **Фаза 4 YAML-часть (цветовые схемы)** — без сервиса, как справочник для будущей логики.

---

# ЭТАП 2 (через ≥3 месяца)

Вынесено сознательно: не в scope текущего цикла.

## Фаза 9. Face & Beauty

- [ ] Макияж по подтипу + цветотипу (стрелки, матовые тени, помада от средних до ярких)
- [ ] Причёска: длина, укладка, что убивает типаж («угольные локоны» vs мягкие)
- [ ] `config/rules/beauty_rules/<subtype>.yaml`
- [ ] API `/beauty-recommendations`
- [ ] Фронт — страница «Твоя внешность: макияж и волосы»
- [ ] Do/Don't фото-примеры (как в PDF стр. 59–66)
- [ ] Правило «где линия — там внимание»

## Фаза 10. Определение пола и мужская версия

- [ ] Gender detection на фото (DeepFace / MediaPipe + classifier)
- [ ] Флаг `ENABLE_MALE_MODE` в `core/config.py`
- [ ] Бизнес-решение: идём ли на мужской рынок (см. `.business/audience/` — сейчас ICP только женский)
- [ ] Если да — отдельная типология, правила, маркетплейсы. Оценка: ≥3 месяца экспертной работы только на правила
- [ ] Если нет — показывать «пока только женский режим» при детекции мужского лица

## Фаза 11. Периферийное (бренд-слой)

- [ ] Шрифты подтипа для соцсетей (в `.business/assets/`, не продукт)
- [ ] Галерея звёзд как контент для маркетинга (переиспользуется из Фазы 8)

---

## Итоговый блок

### Реализовано в сессии 1 (2026-04-21, утро)
- Каркас [reference_looks/flamboyant_gamine.yaml](../ai-stylist-starter/config/rules/reference_looks/flamboyant_gamine.yaml) — 5 референсных луков
- Этот план (Этап 1 из 8 фаз + Этап 2 из 3 фаз)
- Запись о `reference_looks/` в [CLAUDE.md](../CLAUDE.md#L103)

### Реализовано в сессии 2 (2026-04-21, вечер) — массовое наполнение YAML Волны 1 Трека B

Всё наполнение по контенту Гамин-Драматик (FG) из PDF; для остальных 12 подтипов — placeholder-структуры.

**Фаза 1 (силуэт):**
- [silhouette_rules.yaml](../ai-stylist-starter/config/rules/silhouette_rules.yaml) — prefer/avoid/composition_rules для FG

**Фаза 2 (категории):** 14 файлов в [category_rules/](../ai-stylist-starter/config/rules/category_rules/)
- jackets, dresses, sweaters, swimwear, pants, skirts, blouses
- outerwear (с разбивкой по sub_category: coat / fur_coat / puffer / biker_jacket / trench)
- headwear, shoes, bags, belts, eyewear, jewelry, hosiery

**Фаза 3 (стоп-листы):**
- [stop_lists/flamboyant_gamine.yaml](../ai-stylist-starter/config/rules/stop_lists/flamboyant_gamine.yaml) — 6 осей (silhouette, details, fabrics, prints, color, categories_ref) с `reason` на каждое правило

**Фаза 4 (цветовая теория):**
- [color_schemes.yaml](../ai-stylist-starter/config/rules/color_schemes.yaml) — 6 схем + composition_rules для FG + forbidden_palettes

**Фаза 6 (стили):**
- [styles.yaml](../ai-stylist-starter/config/rules/styles.yaml) — 8 стилей с core_items, impression, occasion, color_hints
- [style_subtype_affinity.yaml](../ai-stylist-starter/config/rules/style_subtype_affinity.yaml) — FG-матрица + score_modifiers

**Фаза 8 (identity DNA):**
- [identity_subtype_profiles.yaml](../ai-stylist-starter/config/rules/identity_subtype_profiles.yaml) — полное FG (associations, motto, philosophy, key_principles, 8 celebrities) + placeholder для 12 подтипов

**Валидация:** все 22 YAML-файла прошли `yaml.safe_load`.

### Реализовано в сессии 3 (2026-04-22) — Волна 2: код Фаз 0 и 5 параллельно

Параллельный запуск в worktree'ях: Агент A (Фаза 0) и Агент B (Фаза 5) через Claude Agent SDK. Оба завершены, rebase поверх main, FF-merged.

**Фаза 0 (атрибуты WardrobeItem + CV):**
- 14 nullable-колонок на `wardrobe_items` (alembic 0009); String + `@validates` whitelist вместо `sqlalchemy.Enum` — расширение словаря без миграции enum-типа
- [app/models/item_attributes.py](../ai-stylist-starter/app/models/item_attributes.py) — единый источник whitelist-ов для валидатора и CV
- [garment_recognizer.py:recognize_extended](../ai-stylist-starter/app/services/garment_recognizer.py) — 14 эвристик через общий `_precompute_signals`, детерминированный seed
- 26 тестов; upgrade+downgrade+upgrade на Postgres 16 чисто; head миграций — `0009_wardrobe_item_14_attributes`
- Архитектурное уточнение: `cv_feature_extractor.py` — анализ лица/позы пользователя; CV по одежде корректно живёт в `garment_recognizer.py`

**Фаза 5 (color try-on):**
- [services/color_try_on_service.py](../ai-stylist-starter/app/services/color_try_on_service.py) — rembg один раз на вещь (не на вариант), HSV-shift с сохранением value (тени/блики остаются), детерминированный S3-ключ `color_tryon/<item>/<hex>.webp`
- [routes/color_tryon.py](../ai-stylist-starter/app/api/routes/color_tryon.py) — POST/GET + feedback, `personalization_service.record_color_preference` для обучения
- FASHN-путь stub за флагом `enable_ml_color_tryon=False` — деньги не тратим
- Фронт: [app/(app)/wardrobe/[id]/color-tryon](../frontend/src/app/(app)/wardrobe/[id]/color-tryon/) + [components/color-tryon/ColorTryOnGallery.tsx](../frontend/src/components/color-tryon/ColorTryOnGallery.tsx)
- 16 unit-тестов; интегрирован с `personalization_service`
- TODO-хук: после Фазы 0 добавить проверку `item.pattern_scale is None and item.fabric_finish != 'metallic'` — одной строкой

**Верификация на main после обоих мёржей:** 644 passed + 7 xfailed, tsc `--noEmit` zero errors.

### Реализовано в сессии 4 (2026-04-24) — Волна 2 движков

Все три оставшихся движка Волны 2 закрыты одной сессией. Суммарно 719 passed (было 682, +37 новых тестов).

- **Фаза 1 движок:** [silhouette_rules_service.py](../ai-stylist-starter/app/services/silhouette_rules_service.py) + scorer-обёртка [outfits/scoring/silhouette_rules.py](../ai-stylist-starter/app/services/outfits/scoring/silhouette_rules.py) зарегистрирована в `OutfitScorer._DEFAULT_SCORERS` (вес 0.10). 10 детекторов на атрибутах Фазы 0: prefer/avoid/composition_rules per-subtype. Цветовые правила (`broken_by_color`, `unbroken_vertical`) сознательно отданы `ColorCombinationService`. 15 unit-тестов.
- **Фаза 2 движок:** [category_rules_service.py](../ai-stylist-starter/app/services/category_rules_service.py) + scorer-обёртка [outfits/scoring/category_rules.py](../ai-stylist-starter/app/services/outfits/scoring/category_rules.py) (вес 0.10). Алиасы категорий (jeans→pants, blazer→jackets, coat/trench/puffer→outerwear и т.п.), универсальный prefer-обсчёт (списки/скаляры/sub_types/pending_-атрибуты), stop как справочник reasons. 12 unit-тестов.
- **Фаза 6 движок:** [outfits/scoring/style_affinity.py](../ai-stylist-starter/app/services/outfits/scoring/style_affinity.py) (вес 0.08). Читает `style_tags` вещей, применяет `style_subtype_affinity.yaml` + `score_modifiers`, поддерживает `context.selected_style` как фильтр тегов (готов к UI-селектору). 10 unit-тестов.
- **Аудит плана:** `color_combination_service` (Фаза 4) и `reference_matcher` (Фаза 7) оказались уже реализованы в предыдущих сессиях — пометки в плане синхронизированы с кодом.
- **Волна 3 (часть): stop-predicates.** Расширен формат stop-правил в YAML через опциональное поле `match` (AND-семантика по атрибутам, списки = «любое из»). Сервис проверяет `match` и начисляет штраф `_STOP_PENALTY=0.15` за каждое сработавшее правило.
  - Заполнены predicates для FG в **15 YAML** (все категории): [jackets](../ai-stylist-starter/config/rules/category_rules/jackets.yaml), [pants](../ai-stylist-starter/config/rules/category_rules/pants.yaml), [shoes](../ai-stylist-starter/config/rules/category_rules/shoes.yaml), [dresses](../ai-stylist-starter/config/rules/category_rules/dresses.yaml), [skirts](../ai-stylist-starter/config/rules/category_rules/skirts.yaml), [blouses](../ai-stylist-starter/config/rules/category_rules/blouses.yaml), [sweaters](../ai-stylist-starter/config/rules/category_rules/sweaters.yaml), [bags](../ai-stylist-starter/config/rules/category_rules/bags.yaml), [belts](../ai-stylist-starter/config/rules/category_rules/belts.yaml), [jewelry](../ai-stylist-starter/config/rules/category_rules/jewelry.yaml), [eyewear](../ai-stylist-starter/config/rules/category_rules/eyewear.yaml), [headwear](../ai-stylist-starter/config/rules/category_rules/headwear.yaml), [swimwear](../ai-stylist-starter/config/rules/category_rules/swimwear.yaml), [outerwear](../ai-stylist-starter/config/rules/category_rules/outerwear.yaml), [hosiery](../ai-stylist-starter/config/rules/category_rules/hosiery.yaml) — **~37 правил с `match`**, остальные оставлены справочными (требуют атрибутов сверх Фазы 0: `distressed_level`, `material=lace_layered`, `bow_count` и т.п.).
  - +16 тестов в [test_category_rules_service.py](../ai-stylist-starter/tests/test_category_rules_service.py). Полный набор: **735 passed**.

### Реализовано в сессии 5 (2026-04-25) — массовое наполнение контента 12 подтипов

После Волны 2 движков (сессия 4) главный блокер качества рекомендаций: 12 из 13 подтипов возвращали quality=low с нейтральным score из всех движков (силуэт, категории, стили, стоп-листы), потому что FG был единственным наполненным. Контент-наполнение критично для беты — 92% юзеров получали бы общие рекомендации семейства вместо точных по подтипу. Сессия закрыла этот долг для 4 ключевых поверхностей.

**Фаза 1 (силуэт):**
- silhouette_rules.yaml для 12 подтипов: per-subtype prefer (4–7 атрибутов), avoid (4–6 флагов), composition_rules (4–5 правил с boost/penalty). Методология Kibbe + FG как образец.

**Фаза 2 (категории, топ-4):**
- jackets.yaml, dresses.yaml, pants.yaml, shoes.yaml: 12 подтипов × prefer-блоки + stop с `match`-predicates (AND-семантика).
- Осталось 11 категорий: bags, belts, blouses, eyewear, headwear, hosiery, jewelry, outerwear, skirts, sweaters, swimwear.

**Фаза 3 (стоп-листы):**
- stop_lists/<subtype>.yaml × 12 файлов (по образцу FG): 5 осей (silhouette, details, fabrics, prints, color), 3–5 правил на ось, каждое с `reason` для explainer.

**Фаза 6 (стили):**
- style_subtype_affinity.yaml для 12 подтипов: 8 стилей × excellent/good/neutral/avoid. Никаких neutral-плейсхолдеров.

**Тесты:** обновлены 3 теста (`test_placeholder_subtype`/`test_neutral_placeholder_subtype`) — переименованы в `*_unknown_subtype_returns_neutral` и проверяют fallback на несуществующий подтип, а не на конкретный (теперь все 13 наполнены). pytest: **782 passed**.

**Коммит:** `32f87e3`. Изменений в коде сервисов или API нет — чистое контент-наполнение.

### Реализовано в сессии 7 (2026-04-26) — nested rules в category_rules_service

После сессии 6 (контент outerwear nested) сервис `category_rules_service` всё ещё читал только плоский `rules[subtype].prefer/stop`, поэтому для outerwear с разбивкой `coat/fur_coat/puffer/biker_jacket/trench` все 13 подтипов возвращали fallback (quality=low, score=0). Сессия 7 закрыла этот разрыв.

**Изменения в [category_rules_service.py](../ai-stylist-starter/app/services/category_rules_service.py):**
- `_is_nested_block(block)` — детектор: верхний уровень без `prefer/stop`, но содержит dict-значения с `prefer/stop` → nested
- `_resolve_sub_category(item, subtype_block, file_stem)` — определение sub_category по приоритету: `item.sub_category` → `item.sub_type` → `item.category` (с lower-case-сравнением с ключами nested-блока) → `_DEFAULT_SUBCATEGORY[file_stem]` (для outerwear = "coat")
- `score_item` спускается на уровень sub_category, если блок nested. Для unknown sub_category — fallback на дефолт файла, чтобы не возвращать low-quality для типичных вещей (например, `category="outerwear"` без sub_type).
- Бонус: обработка `prefer.always: true` (выставлено в biker_jacket для FG/Gamine/SG) — явный буст +0.20 без штрафов

**Тесты:** +9 в [test_category_rules_service.py::TestNestedOuterwearRules](../ai-stylist-starter/tests/test_category_rules_service.py): coat для Dramatic (prefer + stop с match), trench через category-алиас, sub_type='trench' внутри outerwear, fallback на coat при unknown sub_category, `always: true` для biker_jacket FG, fur_coat для SD, puffer для Natural, fallback puffer→coat для Dramatic. **791 passed** (было 782).

**Эффект:** outerwear-вещи теперь оцениваются по специфичным правилам своей sub_category вместо fallback. Для типичной зимней верхней одежды (пальто, тренч, пуховик, косуха, шуба) сервис возвращает осмысленный score для всех 13 подтипов.

### Реализовано в сессии 6 (2026-04-25, вечер) — закрытие Фазы 2 для 12 подтипов

После сессии 5 (топ-4 категории) оставались 11 категорий с FG-only наполнением и пустыми stub'ами для 12 подтипов. Если рекомендатор запрашивал blouses/sweaters/skirts/etc. для не-FG юзера, `category_rules_service` возвращал нейтральный score (movement quality=low), что обнуляло вклад движка в финальный score.

**Закрытые YAML (11 файлов × 12 подтипов = 132 блока):**
- bags, belts, blouses, eyewear, headwear, hosiery, jewelry, outerwear, skirts, sweaters, swimwear
- Каждый блок: prefer (4–8 атрибутов: shape/fit/length/details/fabric/color_policy/pending_*) + stop с `reason` (3–5 правил)
- Часть stop-правил имеет structured `match`-predicates (AND-семантика); остальные — справочные (требуют атрибутов сверх Фазы 0)
- outerwear: nested `coat` для всех 12 подтипов + дополнительные sub_categories (trench для Classic family, fur_coat для SD/Romantic/TR, biker_jacket для Gamine family, puffer для Natural)
- Контент выведен из `silhouette_rules.yaml` (характеристики семейств) + методологии Kibbe; FG-блоки сохранены без изменений

**Тесты:** **782 passed**, валидация — все 15 файлов парсятся, все 13 подтипов заполнены, нет пустых stub'ов.

**Эффект:** теперь `category_rules_service` возвращает осмысленный score для всех 13 подтипов во всех 15 категориях (а не только в 4). До сессии 6: 12 подтипов × 11 категорий = 132 пары возвращали fallback. После: 0.

### Реализовано в сессии 8 (2026-04-26) — Identity DNA в explainer

После сессий 6+7 контент Identity DNA (`identity_subtype_profiles.yaml`) был полным для всех 13 подтипов, но `explainer.py` его не использовал — тексты рекомендаций были обезличенные ("Цвет хорошо подходит вам"), без отсылки к стилистической идентичности юзера.

**Изменения в [explainer.py](../ai-stylist-starter/app/services/explainer.py):**
- `_load_profiles()` — кэш-load YAML в память
- `get_identity_profile(subtype)` — полный профиль (philosophy, key_principles, celebrity_examples, associations, motto)
- `identity_intro(subtype) -> {"display_name", "motto", "associations"[:3]}` — компактный intro для UI
- `explain_outfit(outfit, *, subtype=None)` — при `subtype + score≥0.55` фраза "В вашем стиле — оторва, креативная" вставляется первой в `reasons` (не выше `MAX_REASONS=3`)
- `explain_shopping(result, *, subtype=None)` — при `subtype + decision=buy` фраза "Поддержит ваш стиль: <ассоциации>" вставляется первой
- Identity-фразы НЕ добавляются при низком score / decision=skip — нечестно "комплиментовать" плохие варианты

**Интеграция:**
- [today_service.py](../ai-stylist-starter/app/services/today_service.py) — `select_from_pool` и `_pick_for_label` принимают `subtype` из `user_context["identity_family"]` и пробрасывают в `explain_outfit`
- [routes/shopping.py](../ai-stylist-starter/app/api/routes/shopping.py) — `subtype` извлекается из user_context и передаётся в `explain_shopping`

**Тесты:** +21 в новом [test_explainer.py](../ai-stylist-starter/tests/test_explainer.py) — identity_intro для всех 13 подтипов + unknown/None/empty fallback, кэш YAML, регрессия `explain_outfit` без subtype, identity-фраза при высоком/низком score, MAX_REASONS не превышается, shopping для buy/skip. **812 passed** (было 791).

### Реализовано в сессии 9 (2026-04-26) — HSV guard в color_try_on

После Фазы 0 (атрибуты Item) оставался TODO в color_try_on_service: HSV-shift даёт визуальные артефакты на принтах и металлике, но guard в коде стоял заглушкой. Сессия 9 закрыла этот пункт.

- `ColorTryOnService._is_hsv_suitable(item)` — staticmethod, проверяет `pattern_scale is None and fabric_finish != 'metallic'` (case-insensitive для metallic)
- При не-пригодной вещи и выключенном ML — `ColorTryOnResponse(variants=[], quality='low')` + info-лог
- При ML-флаге on — guard не блокирует (FASHN расскрасит принт; пока stub)
- Back-compat: legacy-вещи без атрибутов Фазы 0 (`getattr` → None) считаются пригодными
- +6 тестов в `TestHsvSuitabilityGuard`: patterned, metallic, case-insensitive, solid_matte passes, ML-flag bypass, legacy без атрибутов. **818 passed** (было 812)

### Реализовано в сессии 10 (2026-04-26) — reference_matcher → gap_analysis

После сессии 8 (Identity DNA в explainer) и сессии 9 (HSV guard) оставался последний backend-пункт: missing_slots реф-луков должны порождать suggestions в gap_analysis.

**Изменения:**
- [schemas/gap_analysis.py](../ai-stylist-starter/app/schemas/gap_analysis.py): расширена `GapSuggestion` опциональными `from_reference_look: str | None` и `slot_hint: str | None`
- [gap_analysis_service.py](../ai-stylist-starter/app/services/gap_analysis_service.py): `__init__` принимает опциональный `reference_matcher` (по умолчанию ленивый импорт `ReferenceMatcher()`); `analyze()` при `user_context["identity_family"]` вызывает matcher и собирает `missing_slots` всех луков → дополнительные suggestions
- Дедупликация по `(category, item)`: если slot повторяется в нескольких луках или совпадает с base-suggestion'ом — не дублируется
- Guard-rail: ошибка matcher'а ловится и логируется, gap_analysis работает дальше без ref-suggestions

**Тесты:** новый [test_gap_analysis_service.py](../ai-stylist-starter/tests/test_gap_analysis_service.py) (+9): regression empty wardrobe / no subtype, missing slots → suggestions, дедупликация в нескольких луках и против base, unknown subtype safe, matcher-exception safe, list-category resolution, schema validation. **827 passed** (было 818).

### Осталось Этап 1
- **Фронт:** UI-селектор стилей (Фаза 6 API готов); проверка карточки «Кто ты стилистически» (Фаза 8 UI закрыта в сессии auth/persona)

### Закрыто, остаётся для истории
- **Reference looks** 12 подтипов × 5 луков с реальными AI-фото — сессия 4 (коммит 88de29c)
- **explainer.py с associations/motto** — сессия 8
- **HSV guard в color_try_on** — сессия 9

### Этап 2
Вынесено на ≥3 месяца: Face & Beauty, Gender detection + мужская версия, бренд-слой.

План живой, актуализировать при каждой сессии по правилам [CLAUDE.md](../CLAUDE.md).
