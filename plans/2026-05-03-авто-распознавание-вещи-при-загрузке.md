# Авто-распознавание вещи при загрузке в гардероб

Дата: 2026-05-03.
Источник: `~/.claude/plans/happy-juggling-pie.md` (утверждённый план чата 2026-05-03).

## Текущий статус (на конец сессии 2026-05-03 21:30)

- ✅ Код фичи реализован полностью, в main, прод-flags включены: `enable_vision_analysis=true`, `use_cv_category_classifier=true`, `category_classifier_provider=openai`. Подтверждено через `GET /api/cv-classifier/config`.
- ✅ Секреты `OPENAI_API_KEY` и `OPENAI_HTTP_PROXY` пользователь добавил в GitHub.
- ⚠️ **Vision-pipeline ещё не работает на проде:** все три схемы прокси (`http://`, `https://`, `socks5://`) дают разные handshake-ошибки. Pipeline gracefully падает на heuristic — UX как до фичи (категория с `confidence=0`, юзер выбирает вручную).

**Блокер для следующей сессии:** запустить новый диагностический endpoint после CI-deploy и определить — это IP whitelist в proxy-seller, неверные креды, или сам прокси выключен.

```bash
curl -sS https://stilist24.com/api/cv-classifier/probe-proxy | python -m json.tool
```

Endpoint вернёт `{ok, schemes: [{scheme, ok, status, latency_s, error_type, error_message}]}` — три попытки на одних и тех же кредах, но с разными префиксами URL. Если все три `ok=false` — проблема не в коде, а в самом доступе.

## Context

Загрузка вещи в гардеробе была трёхступенчатой: пользователь выбирал файл, опционально выбирал категорию из dropdown («Угадаем по фото»), жал «Загрузить вещь». Бэкенд гонял эвристики `recognize_extended()` (8/14 атрибутов из Phase 0 покрыто) и опционально gpt-5-nano для категории. Имени вещи не было.

Цель: один vision-вызов через **GPT-5 mini** возвращает категорию, имя, цвет и 14 структурных атрибутов разом. Фронт автоматически шлёт upload по выбору файла, без кнопки. После ответа пользователь видит редактируемые поля и кнопку «ОК».

## Решения по развилкам

- **Background removal:** rembg + u2net (уже стоит, не меняем).
- **Vision провайдер:** GPT-5 mini через `OPENAI_BASE_URL=https://api.openai.com` + `OPENAI_HTTP_PROXY` на западный HTTP-прокси (proxy-seller.com / ISP Proxies). RU-IP блокировка обходится сетевым прокси, не менять endpoint.
- **Имя вещи:** новая колонка `wardrobe_items.name TEXT NULL` (alembic 0011), не в `attributes_json` — поле user-facing, должно быть searchable.
- **Pre-upload Select категории:** убран. Категория всегда определяется vision'ом, правится только в карточке после ответа.
- **Подтверждение:** явная кнопка «ОК» с diff-PATCH'ом, без auto-save на blur.
- **Прогресс:** псевдо-stepper, переключение по `setInterval(2500ms)` в фоне одного `mutation.isPending`. Под капотом цепочка реально идёт «vision → save nobg → DB», подписи не лгут.

## Фазы

### Фаза 1. Backend [x]

#### 1.1. `OpenAIVisionAnalyzer` в `category_classifier.py` [x]

- `VisionAnalysisResult(category, confidence, name, primary_color, attrs, source, reasoning)` dataclass.
- Новый класс `OpenAIVisionAnalyzer` делит инфру с `OpenAICategoryClassifier`: один httpx-клиент, breaker, retry, downscale до 1024px JPEG.
- `_VISION_SYSTEM_PROMPT` собирается из whitelist'ов в `app/models/item_attributes.py` — единый источник истины. При добавлении нового значения в whitelist промпт автоматически расскажет о нём модели.
- `analyze(image_bytes) -> VisionAnalysisResult` — один POST, парсит расширенный JSON. Невалидные значения (вне whitelist) → None через `validate_scalar` / `validate_style_tags`.
- `_coerce_name` урезает name до 60 символов по последнему пробелу.
- `get_vision_analyzer(settings)` фабрика — None при `enable_vision_analysis=False` или пустом `openai_api_key`.

#### 1.2. Миграция 0011 [x]

[`alembic/versions/0011_wardrobe_item_name.py`](../ai-stylist-starter/alembic/versions/0011_wardrobe_item_name.py): nullable `Text`-колонка `wardrobe_items.name`. В [`app/models/wardrobe_item.py`](../ai-stylist-starter/app/models/wardrobe_item.py) — `Mapped[str | None]`.

#### 1.3. Pipeline `routes/wardrobe.py` [x]

Параллельный CV-pipeline через `asyncio.to_thread`:
1. S3 upload оригинала
2. **Параллельно**: rembg-маска (`recognize_garment`) и vision-вызов (`analyzer.analyze`)
3. nobg PNG → S3 (детерминированный ключ `{key}_nobg.png`)
4. Если vision успешен — берём category, name, primary_color, attrs из его ответа. Иначе fallback на эвристики.
5. `repo.create(name=..., category=..., attributes=v2_dict, structured_attrs=14_attrs_dict)`

Latency сократилась ≈ в 1.5× (rembg ~2-4с + vision ~3-5с теперь идут параллельно вместо sequential).

#### 1.4. PATCH `/wardrobe/{id}` [x]

- Новый `WardrobeItemPatchIn(category?: str, name?: str)` в [`schemas/wardrobe.py`](../ai-stylist-starter/app/schemas/wardrobe.py)
- Новый endpoint `PATCH /wardrobe/{item_id}` принимает оба поля опционально, отправляет diff. Если оба None — no-op без записи.
- Старый `PATCH /wardrobe/{item_id}/category` сохранён для совместимости.
- `WardrobeRepository.create` расширен `name` и `structured_attrs` (плоский dict из 14 Phase-0 атрибутов, валидация на уровне SQLAlchemy `@validates`).

#### 1.5. Settings и env [x]

[`app/core/config.py`](../ai-stylist-starter/app/core/config.py):
- `enable_vision_analysis: bool = False` — отдельный от `use_cv_category_classifier` флаг
- `openai_model: str = "gpt-5-mini"` (был `gpt-5-nano`)
- `openai_http_proxy: str = ""` — сетевой прокси для исходящих vision-вызовов

[`.env.prod.example`](../.env.prod.example) и [`.github/workflows/deploy.yml`](../.github/workflows/deploy.yml) here-doc обновлены: новые env-переменные, новый Secret `OPENAI_HTTP_PROXY`.

#### 1.6. Тесты [x]

[`tests/test_category_classifier.py`](../ai-stylist-starter/tests/test_category_classifier.py): +14 кейсов:
- Vision возвращает все поля (category + name + primary_color + 14 attrs)
- Невалидные значения вне whitelist стираются в None
- `style_tags` фильтруется по STYLE_TAG_VALUES
- Long name обрезается до 60 символов
- `null` для name/color обрабатывается без падения
- `max_completion_tokens` (не `max_tokens`), `response_format: json_object` — guard'ы что код не регрессирует на старый API
- Unparseable response / unknown category → RuntimeError
- Factory: возвращает None при выключенном флаге / без ключа
- Прокси прокидывается через factory в analyzer и classifier
- Пустая строка proxy → None в httpx.Client

### Фаза 2. Frontend [x]

[`WardrobeUploader.tsx`](../frontend/src/components/wardrobe/WardrobeUploader.tsx) переписан целиком:
- Auto-trigger `mutation.mutate(file)` в `FileDropzone.onChange`
- Удалён pre-upload Select категории и связанный state — категория всегда определяется vision'ом
- Удалена кнопка «Загрузить вещь»
- Новый `<UploadProgressStepper>` с 3 шагами «Определяем по фото → Вырезаем фон → Загружаем в гардероб», `setInterval(2500ms)` переключает step внутри `mutation.isPending`
- `LastUploadedReview` — редактируемые поля имени и категории (inline, без скрытого режима), кнопка **«ОК»** собирает diff против исходного `item` и шлёт `PATCH /wardrobe/{id}`. Если ничего не менялось → закрывает карточку без сетевого вызова.

[`lib/api/wardrobe.ts`](../frontend/src/lib/api/wardrobe.ts):
- Новый `updateWardrobeItem(id, {category?, name?})` для расширенного PATCH

[`lib/schemas/index.ts`](../frontend/src/lib/schemas/index.ts):
- `WardrobeItemSchema.name: z.string().nullable().optional()`

### Фаза 3. Verification [x]

- pytest curated CI subset + classifier + reference_matcher → **156 passed** (146 baseline + 14 vision)
- pytest classifier-only → **49 passed**
- `tsc --noEmit` (frontend) → 0 ошибок
- `next build` → clean, `/wardrobe` собрался без регрессий

### Фаза 4. Push в main [x]

Два коммита:
- `9de3ca9 feat(wardrobe): авто-распознавание вещи при загрузке через GPT-5 mini vision` — основная фича (13 файлов, +965/-177)
- `69b58fe feat(openai): сетевой HTTP/SOCKS-прокси для исходящих vision-вызовов` — поддержка `OPENAI_HTTP_PROXY` (5 файлов, +76/-6)

Auto-deploy через `.github/workflows/deploy.yml` → SSH на VPS → `docker compose up -d --build` → `alembic upgrade head` поднимет миграцию 0011 автоматически в `entrypoint.sh`.

### Фаза 5. Rollout [ ] — за пользователем

1. **GitHub Secrets** ([https://github.com/programist515-boop/stilist24/settings/secrets/actions](https://github.com/programist515-boop/stilist24/settings/secrets/actions)) — добавить два:
   - `OPENAI_API_KEY` — ключ OpenAI (`sk-...`)
   - `OPENAI_HTTP_PROXY` — `http://USER:PASS@HOST:PORT` от proxy-seller (Proxy IPv4 / ISP Proxies, локация не РФ)
2. **Поправить флаги** в [`.github/workflows/deploy.yml`](../.github/workflows/deploy.yml) here-doc:
   - `USE_CV_CATEGORY_CLASSIFIER=true`
   - `ENABLE_VISION_ANALYSIS=true`
3. Push (любой пустой коммит) → CI деплой
4. Smoke на `https://stilist24.com/wardrobe` — карточка после загрузки фото должна показать **категорию + имя**

## Критические файлы

| Файл | Действие |
|---|---|
| `ai-stylist-starter/app/services/category_classifier.py` | EDIT — `OpenAIVisionAnalyzer`, `VisionAnalysisResult`, прокси через httpx |
| `ai-stylist-starter/app/api/routes/wardrobe.py` | EDIT — pipeline `upload_item`, новый `PATCH /wardrobe/{id}` |
| `ai-stylist-starter/app/models/wardrobe_item.py` | EDIT — `name: Mapped[str \| None]` |
| `ai-stylist-starter/alembic/versions/0011_wardrobe_item_name.py` | NEW — миграция |
| `ai-stylist-starter/app/repositories/wardrobe_repository.py` | EDIT — `create` принимает `name` и `structured_attrs` |
| `ai-stylist-starter/app/schemas/wardrobe.py` | EDIT — `WardrobeItemOut.name`, `WardrobeItemPatchIn` |
| `ai-stylist-starter/app/core/config.py` | EDIT — `enable_vision_analysis`, `openai_http_proxy`, `openai_model="gpt-5-mini"` |
| `ai-stylist-starter/tests/test_category_classifier.py` | EDIT — 14 новых кейсов |
| `frontend/src/components/wardrobe/WardrobeUploader.tsx` | EDIT — auto-upload, stepper, редактируемая карточка с «ОК» |
| `frontend/src/lib/api/wardrobe.ts` | EDIT — `updateWardrobeItem` |
| `frontend/src/lib/schemas/index.ts` | EDIT — `name` в `WardrobeItemSchema` |
| `.env.prod.example` | EDIT — `OPENAI_HTTP_PROXY`, `ENABLE_VISION_ANALYSIS`, `OPENAI_MODEL` |
| `.github/workflows/deploy.yml` | EDIT — Secret `OPENAI_HTTP_PROXY`, here-doc |

## Хронология rollout-сессии 2026-05-03 (16:00–21:30)

| # | Коммит | Что сделано | Эффект на прод |
|---|---|---|---|
| 1 | `9de3ca9` | feat(wardrobe): vision-pipeline через GPT-5 mini | код в main, флаги OFF — прод как раньше |
| 2 | `69b58fe` | feat(openai): сетевой `OPENAI_HTTP_PROXY` через httpx.Client(proxy=…) | прод как раньше |
| 3 | `92fb757` | docs(plan): план в `plans/` | — |
| 4 | merge `vision/rollout-flags` (`5d02ed7`) | flags ON в deploy.yml + check-script + план | пользователь добавил Secrets, флаги активны на проде |
| 5 | `ab2cbf5` | chore(observability): расширил `/cv-classifier/config` флагами vision/proxy | можно curl'ом проверить что rollout применился |
| 6 | `520bd2c` | docs(openai-proxy): пометил что HTTPS-прокси требует `https://` схемы | пользователь обновил секрет на `https://...` |
| 7 | `abd84f9` | fix(openai-proxy): автоматический http→https при `RemoteProtocolError` | юзеру не надо вручную менять схему |
| 8 | `9490913` | feat: добавил `httpx[socks]` в depends + дока про SOCKS5 | контейнер пересобрался с поддержкой socks5 |
| 9 | `77a9dd6` | feat(openai-proxy): полный каскад http → https → socks5 в коде | любой префикс в секрете — код сам перебирает |
| 10 | `8a99a5d` | feat(openai-proxy): timeout 5s на handshake-фазе + `/probe-proxy` диагностика | нет 504 из-за длинного каскада, есть точная диагностика |

## Что увидели в `/cv-classifier/recent` на каждой итерации

| Схема прокси | exception_type | exception_message | Что говорит |
|---|---|---|---|
| `http://` | `RemoteProtocolError` | `'illegal request line'` | прокси отвечает не-HTTP байтами |
| `https://` | `ConnectError` | `'[SSL] record layer failure (_ssl.c:1016)'` | TLS handshake не проходит |
| `socks5://` | `ProtocolError` | `'Malformed reply'` | SOCKS5-handshake тоже не проходит |

**Вывод:** все три схемы провалились с разными ошибками. Это означает **проблема не в схеме URL**, а в самом доступе к прокси-серверу. Самые вероятные причины:
1. **IP whitelist в proxy-seller** — VPS IP `194.67.99.214` не добавлен в whitelist прокси.
2. **Неверные креды** login/password в URL.
3. **План прокси неактивен** — закончился срок, недостаточно баланса.

## Action items для следующей сессии

1. **Дождаться завершения CI-deploy коммита `8a99a5d`** (~5 минут после конца сессии).
2. **Дёрнуть диагностический endpoint:**
   ```bash
   curl -sS https://stilist24.com/api/cv-classifier/probe-proxy | python -m json.tool
   ```
   Endpoint пробует все 3 схемы независимо, возвращает status + error_type + error_message по каждой.
3. **Решить блокер по результату:**
   - Если хоть одна схема `ok: true` → `_proxy_resolved` подхватит её, vision заработает на следующем upload.
   - Если все три `ok: false` с одной и той же ошибкой → пользователю надо проверить в proxy-seller:
     - **IP whitelist:** добавить `194.67.99.214` в список разрешённых IP для этого прокси (отдельно от страницы Developers API!).
     - **Креды:** убедиться что login/password в секрете GitHub совпадает с тем, что в кабинете proxy-seller (можно нажать «Generate new password» и обновить секрет).
     - **План:** активен ли (страница Orders).
4. **После исправления — empty commit для redeploy:**
   ```bash
   git commit --allow-empty -m "redeploy: pick up updated proxy config" && git push
   ```
5. **Финальная проверка:**
   ```bash
   curl -sS 'https://stilist24.com/api/cv-classifier/probe-proxy' | python -m json.tool
   # Ожидание: ok=true, хотя бы одна схема status=200, model_count > 0
   ```
   После этого реальный upload через UI — категория и имя должны заполняться.

## Критические файлы

| Файл | Действие |
|---|---|
| `ai-stylist-starter/app/services/category_classifier.py` | NEW — `OpenAIVisionAnalyzer`, `VisionAnalysisResult`, каскад прокси, helper `_post_with_proxy_fallback`, `_next_proxy_scheme` |
| `ai-stylist-starter/app/api/routes/wardrobe.py` | EDIT — параллельный pipeline (rembg + vision), новый `PATCH /wardrobe/{id}` |
| `ai-stylist-starter/app/api/routes/cv_classifier_observability.py` | EDIT — `/config` расширен флагами, новый `/probe-proxy` |
| `ai-stylist-starter/app/models/wardrobe_item.py` | EDIT — колонка `name` |
| `ai-stylist-starter/alembic/versions/0011_wardrobe_item_name.py` | NEW — миграция |
| `ai-stylist-starter/app/repositories/wardrobe_repository.py` | EDIT — `create(name=, structured_attrs=)` |
| `ai-stylist-starter/app/schemas/wardrobe.py` | EDIT — `WardrobeItemOut.name`, `WardrobeItemPatchIn` |
| `ai-stylist-starter/app/core/config.py` | EDIT — `enable_vision_analysis`, `openai_http_proxy`, `openai_model="gpt-5-mini"` |
| `ai-stylist-starter/pyproject.toml` | EDIT — `httpx[socks]>=0.27.0` |
| `ai-stylist-starter/tests/test_category_classifier.py` | EDIT — 18 новых кейсов (vision + proxy cascade) |
| `frontend/src/components/wardrobe/WardrobeUploader.tsx` | REWRITE — auto-upload, псевдо-stepper, редактируемая карточка с «ОК» |
| `frontend/src/lib/api/wardrobe.ts` | EDIT — `updateWardrobeItem` |
| `frontend/src/lib/schemas/index.ts` | EDIT — `name` в `WardrobeItemSchema` |
| `.env.prod.example` | EDIT — `ENABLE_VISION_ANALYSIS`, `OPENAI_HTTP_PROXY`, `OPENAI_MODEL` |
| `.github/workflows/deploy.yml` | EDIT — Secret `OPENAI_HTTP_PROXY`, флаги ON, `OPENAI_MODEL=gpt-5-mini` |
| `scripts/check_openai_proxy.py` | NEW — smoke-script (можно запустить на сервере с переданными env) |

## Что переносится в следующие планы

После того как vision реально заработает:
- **Калибровка prompt'а** на реальных пользовательских фото (если accuracy < 80%)
- **Few-shot examples** для сложных категорий (jewelry vs bags vs belts, swimwear vs blouses)
- **Метрика `vision_correction_rate`** (сколько юзеров правят категорию или имя через «ОК»-форму)
- **Удалить `_proxy_https_attempted` флаг** из обоих классов (legacy после внедрения каскада, оставлен для test-back-compat)

## Резюме для нового чата

> Vision-pipeline (auto-upload + GPT-5 mini для распознавания вещи и имени + каскадный fallback прокси) реализован и в main, флаги ON. Текущий блокер: прокси proxy-seller отвергает запросы по всем трём схемам (`http`/`https`/`socks5`) с разными handshake-ошибками — `RemoteProtocolError`, `SSL record layer failure`, `Malformed reply`. Это указывает на проблему не схемы URL, а самого доступа: скорее всего IP whitelist пустой или не включает VPS `194.67.99.214`. Дёрнуть `GET /api/cv-classifier/probe-proxy` после завершения CI-деплоя коммита `8a99a5d` — endpoint вернёт точные коды ошибок по каждой схеме. Решение либо в кабинете proxy-seller (whitelist/креды/план), либо смена провайдера.
