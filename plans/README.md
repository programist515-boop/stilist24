# plans/

Технические планы реализации. Здесь живёт информация **как** мы делаем проект.

## Правила (из корневого CLAUDE.md)

1. Один план = одна функция. Если план уже есть — работаем с ним.
2. Имя файла: `YYYY-MM-DD-название-функции.md`.
3. План делится на фазы. У каждой фазы статус `[ ]` или `[x]`.
4. В конце плана — итоговый блок: реализован целиком или нет, что осталось.
5. Любой агент обязан актуализировать план после каждой сессии.

## Исторический roadmap

До введения этой папки roadmap жил в `NEXT_STEPS.md` — перемещён в `plans/archive/NEXT_STEPS.md` (9 шагов: DB → wardrobe → scoring → outfit generation → feedback → personalization → try-on → today → weekly insights). Большая часть уже реализована в `ai-stylist-starter/`. Новые планы пишем здесь.
